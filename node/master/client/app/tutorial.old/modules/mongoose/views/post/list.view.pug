h4 List of posts.
button.btn.btn-primary(ng-click="PostCtrl.loadPosts()") Reload posts.
span=" "
button.btn.btn-primary(ng-click="PostCtrl.drop()") Remove posts collection.
br
br
ul.w3-ul
   li(ng-repeat="post in PostCtrl.posts", style="margin-bottom: 9px").w3-red
      h5 {{ post.title }}
      div.w3-container.w3-pale-red
         json-formatter(json="post", open="true")
      br
      button.btn.btn-primary.btn-xs(ng-click="PostCtrl.remove(post)") Remove post.
      br


