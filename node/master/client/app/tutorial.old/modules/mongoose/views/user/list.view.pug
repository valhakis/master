h4 List of users.
button.btn.btn-primary(ng-click="UserCtrl.loadUsers()") Reload users.
br
br
ul.w3-ul
   li(ng-repeat="user in UserCtrl.users", style="margin-bottom: 9px").w3-red
      h5 {{ user.email }}
      div.w3-container.w3-pale-red
         json-formatter(json="user", open="true")
      br
      button.btn.btn-primary.btn-xs(ng-click="UserCtrl.remove(user)") Remove user.
      br

