p This is tutorial 01.

div.app-group-container
   div.app-group Name: 
      input.app-input(type="text", ng-model="name")
      span {{ ' Hello ' + name }}

   div.app-group(ng-init="color = '#FF0000'") Color: 
      input.app-input(style="background-color: {{ color }}", value="{{color}}", type="text", ng-model="color")
      span {{ ' Hello ' + name }}

   div.app-group(ng-init="quantity = 23; price = 5")
      table.table.table-bordered(style="width: 150px; margin: 0px")
         tbody
            tr
               th Quantity: 
               td {{ quantity }}
            tr
               th Price: 
               td {{ price }}
            tr
               th Total in dollars: 
               td {{ quantity * price + ' $'}}

   div.app-group(ng-init="points=[3, 5, 2, 8, 9]")
      span Points: 
         span(ng-repeat="point in points") {{ point + ' '}}

   div.app-group(ng-init="person={name: 'William', lname: 'Valhakis', age: 23}")
      span Person: {{ person.name + ' ' + person.lname + '; Age: ' + person.age }}

   div.app-group
      h4 Persons.
      table.table.table-bordered(style="width: 200px")
         thead
            tr
               th Index
               th First name
               th Last name
         tbody
            tr(ng-repeat="person in persons")
               td(style="text-align: center;") {{ $index }}
               td {{ person.fname }}
               td {{ person.lname }}

   div.app-group
      span Email: {{ ' ' }}
      form(name="form", style="display: inline;")
         input.app-input(type="email", name="email", ng-model="text")
      span(ng-show="form.email.$error.email")  This is not a valid email.
