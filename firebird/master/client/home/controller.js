module.exports = function controller($http, api) {
   /*
   $http.get(`${api}/api/users.json`).then(function(res) {
      console.log(res.data);
      var users = res.data;
      for (var i=0; i<users.length; i++) {
         console.log(`[${i}] -> `, users[i]);
      }
   });

   var user = {
      name: "William Valhakis",
      email: "william.valhakis@gmail.com",
   };

   $http.post(`${api}/api/users.json`, user).then(function(res) {
      console.log(res.data);
   });
   */
   /*
   $http.get('/api-json/todos.json').then(function(res) {
      console.log(res.data);
   });
   */
};
